import { Story, Meta } from '@storybook/react'
import ContainerMapa from '.'

export default {
  title: 'ContainerMapa',
  component: ContainerMapa
} as Meta

export const Default: Story = () => <ContainerMapa />
